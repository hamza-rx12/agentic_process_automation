apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Pin operator version — bump intentionally.
# Releases: https://github.com/rabbitmq/cluster-operator/releases
resources:
  - https://github.com/rabbitmq/cluster-operator/releases/download/v2.16.0/cluster-operator.yml
  - broker.yaml
